# 项目目标

实现一个运行在本地的 MCP Server，让 Agent 能像调用本地工具一样操作远程服务器。SSH 仅作为服务端内部的传输与执行机制，不向 Agent 暴露 SSH 操作细节。

## 技术栈

- 开发工具链：`go1.27.1`，当前开发环境为 `linux/amd64`。
- 目标平台：跨平台，不局限于 Linux 或 amd64；实现时避免不必要的平台依赖。

## 核心体验

- Agent 通过服务器列表查找目标服务器，再调用 MCP 工具完成远程命令执行、文件读写、目录列举、文件上传下载和持久化终端操作。
- 工具的参数与返回结果应接近本地工具的使用习惯，Agent 不需要生成 SSH 命令、管理连接或处理认证。
- 工具操作默认发生在配置的远程服务器，而不是运行 MCP Server 的本地机器；明确这一语义，避免误操作。
- “Agent 不感知 SSH”指屏蔽传输细节，不是隐瞒操作目标。工具应清晰说明远程执行语义。

## 架构边界

```text
Agent → MCP 协议 → 本地 MCP Server → SSH → 远程服务器
```

- **Agent**：表达操作意图，提供命令、路径、文件内容等业务参数。
- **本地 MCP Server**：提供工具接口，读取连接配置，管理 SSH 连接，将操作转发到远程服务器，并规范化结果与错误。
- **远程服务器**：实际执行命令、访问文件；权限由配置的远程用户及服务器策略决定。
- SSH 地址、端口、用户名、认证方式和主机密钥校验等由本地配置管理，不要求 Agent 在每次调用时提供。
- Agent 可以查找并选择服务器，通过稳定的服务器标识指定操作目标；无需自行建立 SSH 连接或处理认证。

## 要实现的工具

以下为已确定的能力范围。服务器列表和持久化终端的具体工具名称、拆分方式及参数在实现时确定，其余工具使用下列名称。

1. **服务器列表**
   - 供 Agent 查找可用服务器，获取稳定的服务器标识及必要的名称、描述等信息。
   - 其他远程工具通过服务器标识选择目标，连接与认证由 MCP Server 内部完成。
   - 不返回密码、私钥等认证秘密；新增服务器通过单独的添加服务器工具完成，删除等其他管理能力暂不在已确定范围内。
2. **`remote_exec`**
   - 在指定服务器执行命令，支持工作目录和超时，返回标准输出、标准错误及退出码。
   - 用于独立、非持久化的命令调用，不假定多次调用共享 shell 状态；需要保留会话时使用持久化终端。
3. **`remote_read_file`**
   - 读取指定服务器上的文件，支持范围读取和输出大小限制，明确标识截断。
4. **`remote_write_file`**
   - 在指定服务器上创建或覆盖文件，明确覆盖行为及父目录处理规则。
5. **`remote_list_dir`**
   - 列举指定服务器上的目录内容，返回条目名称、类型等必要信息。
   - 对大目录设置返回数量限制，并明确说明是否还有未返回的条目。
6. **`remote_upload` / `remote_download`**
   - `remote_upload`：将运行 MCP Server 的本地机器上的文件上传到指定服务器。
   - `remote_download`：将指定服务器上的文件下载到运行 MCP Server 的本地机器。
   - 参数明确区分本地路径与远程路径；本地不是指可能运行在其他环境中的 Agent 客户端。
   - 明确覆盖策略、传输失败及部分文件的处理方式；不设置 `transfer_root` 配置或传输目录限制，本地文件访问由 MCP Server 进程的操作系统权限控制。本地相对路径基于进程工作目录。
7. **Persistent Terminal（持久化终端，基于 tmux）**
   - 在指定远程服务器上使用 tmux 创建和管理持久化终端，不在本地运行 tmux 来替代远端会话。
   - 支持创建、列举终端，发送命令或输入，读取终端输出，以及显式关闭终端；具体拆分为哪些 MCP 工具待设计。
   - 同一终端保留工作目录、环境变量及运行中的进程，适用于连续操作和长时间任务。
   - SSH 连接断开不应主动销毁 tmux 会话；重连后可重新访问仍然存在的终端，但不承诺跨远端重启存活。
   - 终端标识必须与服务器绑定，避免跨服务器误操作；仅管理本项目拥有的会话，不干扰用户已有 tmux 会话。
   - 远端需要安装 tmux；缺失时明确报错，不静默安装。终端输出是交互式屏幕或历史内容，不等同于 `remote_exec` 的独立 stdout、stderr 和退出码。

8. **添加服务器**
   - 供 Agent 添加服务器连接配置，具体工具名称和参数在实现时确定。
   - 接收服务器名称、地址、端口、用户名及认证配置或凭证引用等必要信息，校验后持久化到本地配置。
   - 返回稳定的服务器标识，添加后可通过服务器列表发现，并供其他远程工具使用。
   - 明确重复配置、配置写入失败及连接验证的处理规则；添加配置成功不等于连接验证成功。
   - 优先使用安全的凭证引用，不在返回值或日志中回显认证秘密；不得自动信任未知主机密钥或跳过校验。

文件精确编辑工具暂不列入已确定的工具列表。

## 实现原则

- 保持工具接口简单、一致，不让 SSH 细节泄漏到业务参数中。
- 明确远程工作目录、相对路径解析及命令会话状态的语义：`remote_exec` 调用相互独立，持久化终端调用在指定 tmux 会话内共享状态。
- 区分命令执行失败、连接失败、认证失败、超时及文件操作失败，返回可供 Agent 判断的错误信息。
- 对输出大小和执行时间设置合理限制，发生截断或超时时明确提示。
- 妥善处理路径、文本和命令参数，避免拼接导致的转义错误或意外命令注入。
- 不在工具返回值或日志中泄漏私钥、密码等敏感信息；不得为了方便而默认跳过 SSH 主机密钥校验。
- 不因远程连接失败而回退到本地执行命令或操作本地文件。
- 工具只使用配置授予的权限，不隐式提权。

## 开发约定

- Go 代码遵循仓库中的 `GO编码规范.md`。
- Git 提交遵循仓库中的 `Git提交规范.md`。
- 实现前先明确 MCP 传输方式、连接配置格式、认证方式和远程工作目录等尚未确定的细节，不将未确认的设计选择视为用户要求。
- 测试应覆盖服务器选择、正常操作、连接异常、超时、输出截断、路径转义、文件传输失败及持久化终端重连等关键场景，并验证操作确实作用于指定远程目标。
