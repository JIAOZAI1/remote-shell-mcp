# Go 编码规范

本文档用于统一 Go 项目的编码风格、工程实践和质量要求。除非项目已有更明确的约定，所有新增及修改代码均应遵循本规范。

## 1. 基本原则

- 优先保证代码正确、清晰、简单和可维护，避免过度设计。
- 遵循 Go 官方惯例，优先使用标准库和项目已有能力。
- 每次修改应聚焦当前需求，避免夹带无关重构或依赖升级。
- 提交前必须格式化代码，并运行与改动范围匹配的测试和静态检查。

## 2. 代码格式

- 所有 Go 文件必须使用 `gofmt` 或 `goimports` 格式化。
- 不通过手工空格实现对齐，不保留无意义的空行和注释掉的废弃代码。
- 合理控制函数长度和嵌套层级；复杂逻辑应拆分为职责清晰的小函数。
- 优先使用提前返回处理异常分支，减少多层嵌套。

```go
func Process(input string) error {
	if input == "" {
		return errors.New("input is empty")
	}

	return doProcess(input)
}
```

## 3. 命名规范

- 包名使用简短、有意义的小写单词，不使用下划线、连字符或无意义缩写。
- 导出标识符使用大驼峰命名，非导出标识符使用小驼峰命名。
- 缩写应保持一致，例如 `ID`、`HTTP`、`URL`，使用 `userID` 而不是 `userId`。
- 接口只有一个方法时，通常使用方法名加 `er` 命名，例如 `Reader`、`Writer`。
- 避免 `util`、`common`、`manager` 等含义宽泛的名称。
- 接收者名称应简短且与类型相关，并在同一类型的方法中保持一致。

```go
type UserService struct{}

func (s *UserService) FindByID(id int64) (*User, error) {
	// ...
}
```

## 4. 包与依赖

- 每个包只承担一组紧密相关的职责，避免循环依赖和“万能包”。
- 包应由使用者按需导入，不应依赖隐式初始化完成复杂业务逻辑。
- 除非确有必要，避免使用 `init`；必须使用时，应保持行为简单、确定且无外部副作用。
- 优先使用标准库。新增第三方依赖前，应评估维护状态、安全性、许可证和引入成本。
- 禁止使用点导入；导入别名仅用于解决名称冲突或提高可读性。

## 5. 函数与方法

- 一个函数只完成一项明确任务，名称应准确表达行为。
- 参数过多时，应考虑使用配置结构体，但不要为少量简单参数制造无意义抽象。
- 不使用布尔参数表达含义不清的模式切换，优先使用具名类型或配置项。
- 能使用值接收者时可以使用值接收者；需要修改状态、避免大对象复制或保持一致性时使用指针接收者。
- 不为了复用少量代码而创建难以理解的通用函数。

## 6. 错误处理

- 所有有意义的错误都必须被处理，不得无理由使用 `_` 忽略。
- 错误信息使用小写开头，末尾不加标点，并描述当前操作。
- 包装错误时使用 `%w` 保留错误链。
- 使用 `errors.Is` 和 `errors.As` 判断错误，不依赖错误字符串。
- 普通业务错误不得使用 `panic`；`panic` 仅用于程序无法恢复的不变量破坏。
- 避免在同一层既记录错误又原样返回错误，以免日志重复。

```go
user, err := repo.FindByID(ctx, id)
if err != nil {
	return nil, fmt.Errorf("find user %d: %w", id, err)
}
```

## 7. Context 使用

- 网络、数据库、文件系统及其他可能阻塞的操作应支持 `context.Context`。
- `context.Context` 通常作为第一个参数传入，命名为 `ctx`。
- 不将 `Context` 保存到结构体中，也不传入 `nil`。
- 调用方负责设置合理的取消或超时策略，并及时调用 `cancel`。
- 不使用 `context.Value` 传递普通业务参数。

```go
ctx, cancel := context.WithTimeout(parentCtx, 3*time.Second)
defer cancel()
```

## 8. 并发规范

- 只有在并发能带来明确收益时才创建 goroutine。
- 每个 goroutine 都应有清晰的退出条件，并能够响应取消信号。
- 明确 channel 的创建方、写入方和关闭方；通常由发送方关闭 channel。
- 不重复关闭 channel，也不向已关闭的 channel 发送数据。
- 共享状态必须通过锁、原子操作或消息传递进行保护。
- 加锁范围应尽可能小，使用 `defer` 解锁时仍需关注临界区大小。
- 并发代码应运行竞态检测：`go test -race ./...`。

## 9. 接口与抽象

- 接口应由使用方定义，而不是由实现方预先设计。
- 接口保持小而专一，优先组合多个小接口。
- 如果只有一个实现且没有测试替换或解耦需求，可以直接依赖具体类型。
- 返回具体类型，按需接收接口。
- 使用泛型前应确认它能显著减少重复并提升类型安全，避免将简单逻辑复杂化。

## 10. 资源管理

- 文件、连接、响应体、计时器等资源创建成功后，应尽快安排释放。
- 对循环或长生命周期代码谨慎使用 `defer`，避免资源延迟释放。
- HTTP 响应体读取完成后必须关闭，并正确处理非成功状态码。
- 数据库事务必须明确提交或回滚；失败路径不得遗留未结束事务。

```go
resp, err := client.Do(req)
if err != nil {
	return fmt.Errorf("send request: %w", err)
}
defer resp.Body.Close()
```

## 11. 日志与可观测性

- 使用项目统一的结构化日志组件，不在业务代码中随意使用 `fmt.Println`。
- 日志应包含定位问题所需的上下文，如请求 ID、资源 ID 和操作名称。
- 禁止记录密码、令牌、密钥、完整证件号码等敏感信息。
- 日志级别应与事件严重程度一致，预期内的业务失败通常不应记录为系统错误。
- 指标名称和标签应保持稳定，避免使用高基数字段作为标签。

## 12. 配置与安全

- 配置应显式加载并在启动阶段完成校验，缺少必要配置时快速失败。
- 密钥和凭证通过安全的配置渠道注入，不得硬编码或提交到仓库。
- 所有外部输入均应进行长度、格式、范围和权限校验。
- 数据库查询应使用参数化语句，禁止拼接不可信输入。
- HTTP 服务应配置合理的读取、写入、空闲和关闭超时。
- 错误响应不应泄露堆栈、内部路径、SQL 或敏感配置。

## 13. 注释与文档

- 注释用于解释设计原因、约束和不明显的行为，不复述代码本身。
- 导出的包、类型、函数和常量应提供符合 Go 文档习惯的注释。
- 导出标识符的注释应以该标识符名称开头。
- `TODO` 应说明待办事项的原因；团队有约定时应关联负责人或任务编号。

```go
// UserService provides operations for managing users.
type UserService struct {
	// ...
}
```

## 14. 测试规范

- 新功能和缺陷修复必须补充相应测试。
- 测试应覆盖正常路径、边界条件和关键错误路径。
- 多组输入输出场景优先采用表驱动测试，并使用清晰的用例名称。
- 测试必须可重复运行，不依赖执行顺序、真实时间、随机外部状态或生产服务。
- 对时间、随机数和外部系统使用可替换依赖或测试替身。
- 断言失败信息应包含实际值与期望值，便于快速定位问题。

```go
func TestNormalizeName(t *testing.T) {
	tests := []struct {
		name string
		in   string
		want string
	}{
		{name: "trim spaces", in: "  Alice ", want: "Alice"},
		{name: "empty value", in: "", want: ""},
	}

	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			got := NormalizeName(tt.in)
			if got != tt.want {
				t.Fatalf("NormalizeName(%q) = %q, want %q", tt.in, got, tt.want)
			}
		})
	}
}
```

## 15. 提交前检查

根据项目实际情况执行以下命令：

```bash
gofmt -w .
go test ./...
go vet ./...
go test -race ./...
```

提交前确认：

- 代码已格式化，命名和结构清晰。
- 所有错误均被正确处理。
- 资源、goroutine 和事务能够可靠退出或释放。
- 新增行为已有测试覆盖。
- 未提交密钥、调试代码、临时文件或无关改动。
- 文档、配置示例和接口说明已随行为变化同步更新。
